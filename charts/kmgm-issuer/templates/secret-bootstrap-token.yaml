{{- if .Values.kmgm.bootstrap.secret.create }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "kmgm.tokenSecretName" . }}
  labels:
    {{- include "kmgm.labels" . | nindent 4 }}
type: Opaque
data:
  token: {{ include "kmgm.bootstrapToken" . | b64enc | quote }}
{{- end }}
